sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t qbanboy14/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true qbanboy14/react-test npm test

after_success:
  - docker build -t qbanboy14/multi-client ./client
  - docker build -t qbanboy14/multi-nginx ./nginx
  - docker build -t qbanboy14/multi-server ./server
  - docker build -t qbanboy14/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push qbanboy14/multi-client
  - docker push qbanboy14/multi-nginx
  - docker push qbanboy14/multi-server
  - docker push qbanboy14/multi-worker
